x-airflow-common: &airflow-common
  build:
    context: .
    dockerfile: Dockerfile.airflow
  image: welding-airflow:3.0.1
  user: "${AIRFLOW_UID:-50000}:0"
  environment:
    &airflow-common-env
    AIRFLOW__CORE__EXECUTOR: LocalExecutor
    # PIP requirements are now in Dockerfile.airflow
    AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://${POSTGRES_USER:-welding}:${POSTGRES_PASSWORD:-welding_pass}@postgres:5432/${POSTGRES_DB:-welding_drift}
    AIRFLOW__CORE__FERNET_KEY: ${AIRFLOW_FERNET_KEY:-}
    AIRFLOW__CORE__LOAD_EXAMPLES: "false"
    AIRFLOW__FAB__AUTH_ROLE_PUBLIC: "Admin"
    AIRFLOW__WEBSERVER__EXPOSE_CONFIG: "True"
    AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: "true"
    AIRFLOW__SCHEDULER__ENABLE_HEALTH_CHECK: "true"
    AIRFLOW_CONN_KAFKA_DEFAULT: kafka://kafka:9092
    AIRFLOW_CONN_SPARK_DEFAULT: spark://spark-master:7077
    AIRFLOW_CONN_WELDING_POSTGRES: postgresql://${POSTGRES_USER:-welding}:${POSTGRES_PASSWORD:-welding_pass}@postgres:5432/${POSTGRES_DB:-welding_drift}
    # Airflow 3 Auth Manager explicit setting
    AIRFLOW__CORE__AUTH_MANAGER: "airflow.providers.fab.auth_manager.fab_auth_manager.FabAuthManager"
    DOCKER_HOST: unix:///var/run/docker.sock
  volumes:
    - ./airflow/dags:/opt/airflow/dags
    - ./airflow/logs:/opt/airflow/logs
    - ./airflow/plugins:/opt/airflow/plugins
    - ./scripts:/opt/airflow/scripts:ro
    - /var/run/docker.sock:/var/run/docker.sock
  networks:
    - welding-net
  depends_on:
    postgres:
      condition: service_healthy
    kafka-init:
      condition: service_completed_successfully
    spark-master:
      condition: service_started

services:
  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.3
    container_name: welding-zookeeper
    restart: unless-stopped
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    volumes:
      - zk-data:/var/lib/zookeeper/data
      - zk-log:/var/lib/zookeeper/log
    networks:
      - welding-net
    healthcheck:
      test: ["CMD-SHELL", "echo srvr | nc localhost 2181 | grep 'Zookeeper version'"]
      interval: 10s
      timeout: 5s
      retries: 10

  kafka:
    image: confluentinc/cp-kafka:7.5.3
    container_name: welding-kafka
    restart: unless-stopped
    depends_on:
      zookeeper:
        condition: service_healthy
    ports:
      - "${KAFKA_EXTERNAL_PORT:-29092}:29092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENERS: INTERNAL://0.0.0.0:9092,EXTERNAL://0.0.0.0:29092
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka:9092,EXTERNAL://${KAFKA_EXTERNAL_HOST:-localhost}:29092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "false"
      KAFKA_NUM_PARTITIONS: 8
      KAFKA_DEFAULT_REPLICATION_FACTOR: 1
      KAFKA_MESSAGE_MAX_BYTES: 5242880
      KAFKA_REPLICA_FETCH_MAX_BYTES: 5242880
      KAFKA_LOG_RETENTION_HOURS: 168
      KAFKA_LOG_SEGMENT_BYTES: 536870912
      KAFKA_HEAP_OPTS: "-Xms512m -Xmx1g"
    volumes:
      - kafka-data:/var/lib/kafka/data
    networks:
      - welding-net
    healthcheck:
      test: ["CMD-SHELL", "kafka-broker-api-versions --bootstrap-server localhost:9092"]
      interval: 15s
      timeout: 10s
      retries: 15
      start_period: 30s

  airflow-dag-processor:
    <<: *airflow-common
    command: dag-processor
    depends_on:
      airflow-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "airflow", "jobs", "check", "--job-type", "DagProcessorJob"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  airflow-triggerer:
    <<: *airflow-common
    command: triggerer
    depends_on:
      airflow-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", 'airflow jobs check --job-type TriggererJob --hostname "$${HOSTNAME}"']
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  kafka-init:
    image: confluentinc/cp-kafka:7.5.3
    container_name: welding-kafka-init
    depends_on:
      kafka:
        condition: service_healthy
    entrypoint: ["/bin/bash", "-ec"]
    command:
      - |
        echo 'Creating Kafka topics'
        kafka-topics --bootstrap-server kafka:9092 --create --if-not-exists --topic welding.raw.v1 --partitions 8 --replication-factor 1 --config retention.ms=604800000 --config compression.type=producer --config max.message.bytes=5242880
        kafka-topics --bootstrap-server kafka:9092 --create --if-not-exists --topic welding.raw.laser_a.v1 --partitions 8 --replication-factor 1 --config retention.ms=604800000 --config compression.type=producer --config max.message.bytes=5242880
        kafka-topics --bootstrap-server kafka:9092 --create --if-not-exists --topic welding.raw.laser_b.v1 --partitions 8 --replication-factor 1 --config retention.ms=604800000 --config compression.type=producer --config max.message.bytes=5242880
        kafka-topics --bootstrap-server kafka:9092 --create --if-not-exists --topic welding.validated.v1 --partitions 8 --replication-factor 1 --config retention.ms=604800000 --config compression.type=producer
        kafka-topics --bootstrap-server kafka:9092 --create --if-not-exists --topic welding.features.v1 --partitions 8 --replication-factor 1 --config retention.ms=2592000000 --config cleanup.policy=compact,delete --config compression.type=producer
        kafka-topics --bootstrap-server kafka:9092 --create --if-not-exists --topic welding.quality_decision.v1 --partitions 4 --replication-factor 1 --config retention.ms=2592000000
        kafka-topics --bootstrap-server kafka:9092 --create --if-not-exists --topic welding.dlq.v1 --partitions 4 --replication-factor 1 --config retention.ms=604800000
        kafka-topics --bootstrap-server kafka:9092 --list
        echo 'Kafka topic initialization done'
    networks:
      - welding-net

  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    container_name: welding-kafka-ui
    restart: unless-stopped
    depends_on:
      kafka:
        condition: service_healthy
    ports:
      - "${KAFKA_UI_PORT:-8089}:8080"
    environment:
      KAFKA_CLUSTERS_0_NAME: welding-local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:9092
      DYNAMIC_CONFIG_ENABLED: "true"
    networks:
      - welding-net

  postgres:
    image: postgres:16
    container_name: welding-postgres
    restart: unless-stopped
    ports:
      - "${POSTGRES_PORT:-15432}:5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-welding_drift}
      POSTGRES_USER: ${POSTGRES_USER:-welding}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-welding_pass}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d:ro
    networks:
      - welding-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-welding} -d ${POSTGRES_DB:-welding_drift}"]
      interval: 10s
      timeout: 5s
      retries: 10

  spark-master:
    build:
      context: .
      dockerfile: Dockerfile.spark
    image: welding-spark:3.5.1
    container_name: welding-spark-master
    restart: unless-stopped
    depends_on:
      kafka-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    ports:
      - "${SPARK_MASTER_PORT:-7077}:7077"
      - "${SPARK_MASTER_UI_PORT:-18080}:8080"
    command:
      [
        "/opt/spark/bin/spark-class",
        "org.apache.spark.deploy.master.Master",
        "--host",
        "spark-master",
        "--port",
        "7077",
        "--webui-port",
        "8080"
      ]
    volumes:
      - spark-events:/opt/spark/events
      - ${DATA_DIR:?set DATA_DIR in .env}:/data:ro
      - ${STORAGE_DIR:?set STORAGE_DIR in .env}:/storage
      - ./spark_batch.py:/opt/spark/apps/spark_batch.py:ro
      - ./spark_streaming.py:/opt/spark/apps/spark_streaming.py:ro
    networks:
      - welding-net

  spark-worker:
    build:
      context: .
      dockerfile: Dockerfile.spark
    image: welding-spark:3.5.1
    container_name: welding-spark-worker
    restart: unless-stopped
    depends_on:
      spark-master:
        condition: service_started
    ports:
      - "${SPARK_WORKER_UI_PORT:-18081}:8081"
    command:
      [
        "/opt/spark/bin/spark-class",
        "org.apache.spark.deploy.worker.Worker",
        "spark://spark-master:7077",
        "--webui-port",
        "8081",
        "--cores",
        "2",
        "--memory",
        "2g"
      ]
    volumes:
      - spark-events:/opt/spark/events
      - ${DATA_DIR:?set DATA_DIR in .env}:/data:ro
      - ${STORAGE_DIR:?set STORAGE_DIR in .env}:/storage
      - ./spark_batch.py:/opt/spark/apps/spark_batch.py:ro
      - ./spark_streaming.py:/opt/spark/apps/spark_streaming.py:ro
    networks:
      - welding-net

  airflow-init:
    <<: *airflow-common
    container_name: welding-airflow-init
    entrypoint: /bin/bash
    command:
      - -c
      - |
        airflow db migrate
    restart: "no"

  airflow-webserver:
    <<: *airflow-common
    container_name: welding-airflow-webserver
    depends_on:
      airflow-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      kafka-init:
        condition: service_completed_successfully
      spark-master:
        condition: service_started
    command: api-server
    ports:
      - "${AIRFLOW_WEBSERVER_PORT:-8080}:8080"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -f http://localhost:8080/api/v2/monitor/health || exit 1",
        ]
      interval: 30s
      timeout: 10s
      retries: 5
    restart: unless-stopped

  airflow-scheduler:
    <<: *airflow-common
    container_name: welding-airflow-scheduler
    depends_on:
      airflow-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      kafka-init:
        condition: service_completed_successfully
      spark-master:
        condition: service_started
    command: scheduler
    restart: unless-stopped

  producer:
    build:
      context: .
      dockerfile: Dockerfile.producer
    container_name: welding-producer
    depends_on:
      kafka-init:
        condition: service_completed_successfully
    environment:
      KAFKA_BOOTSTRAP: kafka:9092
      TOPIC_RAW: welding.raw.v1
      TOPIC_RAW_LASER_A: welding.raw.laser_a.v1
      TOPIC_RAW_LASER_B: welding.raw.laser_b.v1
      DATA_DIR: /data
      STORAGE_DIR: /storage
    volumes:
      - ${DATA_DIR:?set DATA_DIR in .env}:/data:ro
      - ${STORAGE_DIR:?set STORAGE_DIR in .env}:/storage
      - ./producer.py:/app/producer.py:ro
    command:
      [
        "--data-dir",
        "/data",
        "--kafka",
        "kafka:9092",
        "--speed",
        "50"
      ]
    networks:
      - welding-net

volumes:
  zk-data:
  zk-log:
  kafka-data:
  postgres-data:
  spark-events:

networks:
  welding-net:
    driver: bridge
